export enum Provider {
  SUPABASE = 'supabase',
  NEST = 'nest',
}

export enum AccessType {
  PUBLIC = 'public',
  PRIVATE = 'private',
}

export enum PostType {
  ARTICLE = 'article',
  JOURNAL = 'journal',
}

export enum EmotionLevel {
  '0%' = '0%',
  '25%' = '25%',
  '50%' = '50%',
  '75%' = '75%',
  '100%' = '100%',
}
